// @ts-check

/**
 * @type {{Console, Random}} MissionUtils
 */
export { Console, Random } from '@woowacourse/mission-utils';

/**
 * @typedef {object} Console
 * @property {(query: string) => Promise<string>} readLineAsync
 * @property {(message: string) => void} print
 */
